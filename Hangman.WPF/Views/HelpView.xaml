<UserControl x:Class="Hangman.WPF.Views.HelpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="450">
        <TextBlock Text="{Binding HelpTitle}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <TextBlock Text="{Binding HelpLine1}" TextWrapping="Wrap" FontSize="14" Margin="0,10,0,0"/>
        <TextBlock Text="{Binding HelpLine2}" TextWrapping="Wrap" FontSize="14"/>

        <TextBlock Text="{Binding HelpModesTitle}" FontSize="18" FontWeight="Bold" Margin="0,15,0,5"/>
        <TextBlock Text="{Binding HelpModesLine1}" TextWrapping="Wrap" FontSize="14" Margin="10,0,0,0"/>
        <TextBlock Text="{Binding HelpModesLine2}" TextWrapping="Wrap" FontSize="14" Margin="10,0,0,0"/>

        <TextBlock Text="{Binding HelpSourcesTitle}" FontSize="18" FontWeight="Bold" Margin="0,15,0,5"/>
        <TextBlock Text="{Binding HelpSourcesLine1}" TextWrapping="Wrap" FontSize="14" Margin="10,0,0,0"/>
        <TextBlock Text="{Binding HelpSourcesLine2}" TextWrapping="Wrap" FontSize="14" Margin="10,0,0,0"/>
        <TextBlock Text="{Binding HelpSourcesLine3}" TextWrapping="Wrap" FontSize="14" Margin="10,0,0,0"/>

        <Button Content="Tillbaka till Menyn" Command="{Binding BackToMenuCommand}" HorizontalAlignment="Center" Margin="0,30" Padding="10"/>
    </StackPanel>
</UserControl>